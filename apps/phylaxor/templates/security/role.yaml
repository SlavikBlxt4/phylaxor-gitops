apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: phylaxor-enricher-role
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: phylaxor
    app.kubernetes.io/component: enricher
    logging-mode: {{ .Values.logging.mode }}
rules:
  # Base permissions: always allow pods & events queries
  - apiGroups: [""]
    resources: ["pods", "events"]
    verbs: ["get", "list", "watch"]
  # Conditional: pods/log access ONLY when logging.mode=podlogs
  {{- if eq .Values.logging.mode "podlogs" }}
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  {{- end }}
